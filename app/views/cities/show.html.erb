<p style="color: green"><%= notice %></p>

<%= render @city %>
<table class="table table-success table-striped table-bordered">
  <thead >
      <th scope="col">Clientes</th>
    </tr>
  </thead>
  <tbody>
    <% @clients.each do |client| %>
      <tr>
      <td> <%= client.nombre %> </td>
      </tr>
   <% end %>
   </tbody>
</table>

<div>
  <%= link_to "Volver a ciudades", cities_path, class: "btn btn-secondary btn-sm" %>
</div>
