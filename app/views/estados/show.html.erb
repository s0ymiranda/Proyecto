<p style="color: green"><%= notice %></p>

<%= render @estado %>

<table class="table table-success table-striped table-bordered">
  <thead>
      <th scope="col">Ciudades:</th>
    </tr>
  </thead>
  <tbody>
    <% @cities.each do |city| %>
      <tr>
      <td> <%= city.nombre %> </td>
      </tr>
   <% end %>
   </tbody>
</table>

<div>
  <%= link_to "Volver a Estados", estados_path, class: "btn btn-secondary btn-sm" %> 
</div>
